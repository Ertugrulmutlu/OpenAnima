# OpenAnima v1.0.0-rc1 Release Materials

## GitHub Release Notes

# OpenAnima v1.0.0-rc1

OpenAnima is an open-source Windows desktop app for placing local 2D visual assets on your desktop as independent overlay windows.

This release is a `v1.0.0-rc1` release candidate. It focuses on making the existing 2D overlay workflow more reliable, recoverable, and easier to diagnose before the final v1.0 release.

3D support is planned for exploration after v1.0. It is not included in this release.

## Highlights

- Packaged Windows executable build: `OpenAnima.exe`
- More reliable `config.json` loading and saving
- Atomic config writes to reduce risk of broken saved sessions
- Corrupt config recovery with timestamped backups
- Schema versioning for saved config data
- Safer handling for missing, partial, invalid, or old saved configs
- Off-screen overlay recovery during restore
- Recovery tools for hidden, locked, click-through, or off-screen overlays
- System tray emergency actions
- File logging with rotating logs
- Diagnostics tab for version, paths, active overlays, and recent warnings/errors
- README and release checklist updates for v1.0 preparation
- PyInstaller spec-based packaging setup

## Supported Asset Types

OpenAnima v1.0.0-rc1 supports:

- GIF overlays
- Static image overlays: PNG, JPG, JPEG, WEBP
- Frame-folder animations
- Sprite strips
- Spritesheets with metadata-defined animations
- Composite UI / HUD-style layered assets

A bundled demo/easter-egg GIF is included and appears in the Library.

## Recovery And Reset Tools

The Active tab includes recovery actions for situations where users lose control of overlays:

- Bring all overlays to center
- Disable click-through for all overlays
- Unlock all overlays
- Show all overlays
- Hide all overlays
- Clear saved session / reset active overlays

The system tray also includes emergency actions:

- Show Control Panel
- Show all overlays
- Disable click-through for all
- Bring all overlays to center
- Exit

## Diagnostics And Logging

The packaged app runs without a console window, so important warnings and errors are now written to:

```txt
logs/openanima.log
```

The Diagnostics tab shows:

- OpenAnima version
- Config path
- Current asset root
- Log file path
- Active overlay count
- Recent warnings/errors

It also includes buttons to open the logs folder and copy diagnostic info.

## Safer Config Persistence

OpenAnima stores session state in `config.json`.

This release adds:

- Atomic config saves
- `schema_version: 1`
- Safe defaults for missing config files
- Corrupt JSON backup files named like `config.corrupt.YYYYMMDD_HHMMSS.json`
- Per-overlay validation so one bad saved overlay does not break the whole session
- Missing asset handling that skips only the affected overlays

## Download And Run Notes

Download the Windows executable from this GitHub release, then run:

```txt
OpenAnima.exe
```

On first launch, OpenAnima creates local runtime files beside the executable:

```txt
assets/
config.json
logs/
```

If Windows SmartScreen appears, choose the normal "More info" / "Run anyway" flow only if you trust the downloaded file and source.

## Known Limitations

- This is a release candidate, not the final v1.0 release.
- v1.0.0-rc1 is focused on Windows.
- 3D model support is not included.
- Sprite strips may need manual frame count, frame size, or crop correction.
- Spritesheets require metadata or setup through the import wizard.
- Composite UI assets may need manual layer alignment.
- The Composite UI editor is useful but not a full professional layout tool.
- Some third-party asset packs may need manual `asset.json` cleanup.
- Use third-party assets only when their license allows it.

## Testing Requested

If you try this release candidate, useful feedback includes:

- Does the packaged EXE launch cleanly?
- Do overlays restore correctly after restart?
- Do recovery actions work if overlays are hidden, locked, click-through, or off-screen?
- Does the Diagnostics tab show useful paths and warnings?
- Do imported assets behave as expected?
- Does `logs/openanima.log` capture useful information when something fails?

## itch.io Update Text

OpenAnima `v1.0.0-rc1` is now available as a Windows release candidate.

OpenAnima is an open-source desktop overlay app for placing local 2D visual assets on your desktop: GIFs, static images, frame animations, sprite strips, spritesheets, and layered HUD/composite UI assets.

This release candidate focuses on reliability:

- packaged Windows `OpenAnima.exe`
- safer `config.json` persistence with atomic saves
- corrupt config backup and recovery
- recovery tools for hidden, locked, click-through, or off-screen overlays
- system tray emergency actions
- logs in `logs/openanima.log`
- Diagnostics tab with version, config path, asset root, log path, overlay count, and recent warnings

A bundled demo/easter-egg GIF appears in the Library and should remain there.

Known limitations: this is still a release candidate, Windows-focused, and 3D support is not included. 3D support is planned for exploration after v1.0.

Download the EXE, run `OpenAnima.exe`, and use the Control Panel to import or add assets.

## LinkedIn Post

I am preparing OpenAnima `v1.0.0-rc1`, a release candidate for an open-source Windows desktop overlay app.

OpenAnima lets you place local 2D visual assets on your desktop as independent overlays: GIFs, static images, frame-folder animations, sprite strips, spritesheets, and layered HUD-style assets.

This release candidate is focused on reliability rather than new visual scope:

- packaged Windows EXE
- safer config persistence with atomic saves
- corrupt config recovery
- recovery tools for hidden, locked, click-through, or off-screen overlays
- diagnostics panel and rotating log file
- system tray emergency actions
- clearer README and release checklist

3D support is planned after v1.0 and is not included in this release.

If you test the release candidate, the most useful feedback is whether the EXE launches cleanly, overlays restore correctly, recovery actions work, and diagnostics/logs make failures easier to understand.

## Reddit-Friendly Post

Title: OpenAnima v1.0.0-rc1 - open-source Windows desktop overlay app for GIFs, sprites, and HUD-style assets

Hi, I am preparing `v1.0.0-rc1` of OpenAnima.

OpenAnima is an open-source Windows desktop app that lets you place local 2D visual assets directly on your desktop as independent overlay windows.

It supports:

- GIFs
- static images: PNG, JPG, JPEG, WEBP
- frame-folder animations
- sprite strips
- spritesheets with metadata-defined animations
- composite UI / HUD-style layered assets

This release candidate is mostly about making the app safer and easier to recover from problems:

- packaged Windows `OpenAnima.exe`
- atomic `config.json` saves
- corrupt config backup instead of crashing
- safer loading of partial or old configs
- missing saved assets are skipped without breaking the whole session
- overlays restored off-screen are moved back to the primary screen
- recovery tools: center all overlays, disable click-through, unlock all, show/hide all, clear saved session
- tray emergency actions
- logs in `logs/openanima.log`
- Diagnostics tab with version, config path, asset root, log path, overlay count, and recent warnings/errors

Known limitations:

- this is a release candidate, not the final v1.0
- Windows-focused
- 3D support is not included and is planned after v1.0
- sprite/spritesheet/composite assets can still require manual metadata cleanup
- third-party asset packs depend on their own licenses and folder formats

The most helpful testing feedback would be:

- Does the EXE launch cleanly?
- Do overlays restore after restarting?
- Can you recover overlays if they are hidden, locked, click-through, or off-screen?
- Are diagnostics/logs useful when something fails?
- Do imported asset packs behave as expected?

## Website Announcement

OpenAnima `v1.0.0-rc1` is a release candidate for the first stable v1 series.

OpenAnima is an open-source Windows desktop overlay app for local 2D visual assets, including GIFs, static images, frame animations, sprite strips, spritesheets, and layered HUD/composite UI assets.

This release candidate focuses on reliability and release readiness: a packaged Windows EXE, safer config persistence, corrupt config recovery, recovery/reset tools, system tray emergency actions, rotating log files, and a Diagnostics tab.

3D support is planned for exploration after v1.0 and is not included in this release.

Download the Windows build, run `OpenAnima.exe`, and use the Control Panel to import assets or restore control if overlays become hidden, locked, click-through, or off-screen.

## Assumptions

- The GitHub release will include `dist/OpenAnima.exe` built from `OpenAnima.spec`.
- The release remains Windows-focused for `v1.0.0-rc1`.
- The bundled demo/easter-egg GIF remains intentionally included in the Library.
- 3D support should be described only as planned/experimental after v1.0.
